<div class="pos">
  <div class="main_content">
    <h1 class="content-title">Your progress 📈</h1>

    <div class="progress">
        <table class="table_users_1">
          <tbody>
            <tr>
              <td>№</td>
              <td>Date</td>
              <td>Symbols per minute</td>
            </tr>
            <% list_users = Action.all %>
            <% index = 0 %>
            <% list_users.length.times do |i| %>
              <% if list_users[i].user_id == current_user.id %>
                <tr>
                  <td><%= index += 1 %></td>
                  <td><%= list_users[i].created_at %></td>
                  <td><%= list_users[i].gen_results %></td>
                </tr>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
    <div class="progress">
        <table class="table_users_1">
          <tbody>
            <tr>
              <td>№</td>
              <td>Date</td>
              <td>Time</td>
            </tr>
            <% list_users = ActionTime.all %>
            <% index = 0 %>
            <% list_users.length.times do |i| %>
              <% if list_users[i].user_id == current_user.id %>
                <tr>
                  <td><%= index += 1 %></td>
                  <td><%= list_users[i].created_at %></td>
                  <td><%= list_users[i].gen_time %></td>
                </tr>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>